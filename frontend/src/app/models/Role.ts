/**
 * Interface représentant un rôle utilisateur.
 * Utilisée pour récupérer les informations du rôle
 * associé à un membre depuis le backend.
 */
 export interface Role {
  roleId: number;
  name: string;
}
